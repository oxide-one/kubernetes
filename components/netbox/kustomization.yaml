resources:
  - netbox-web
  - netbox-worker
  - netbox-housekeeping
  - namespace.yaml
  
images:
- name: netbox
  newName: ghcr.io/netbox-community/netbox:v3.1

commonLabels:
  app: netbox

namespace: netbox
